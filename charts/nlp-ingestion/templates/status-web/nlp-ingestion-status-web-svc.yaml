apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.nlpingestion.statusweb.service.name }}
  namespace: {{ .Values.nlpingestion.namespace }}
spec:
  ports:
    - port: {{ .Values.nlpingestion.statusweb.service.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.nlpingestion.statusweb.name }}