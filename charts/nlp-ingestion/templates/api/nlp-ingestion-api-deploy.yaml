# NLP API Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.nlpingestion.api.name }}
  namespace: {{ .Values.nlpingestion.namespace | default .Release.Namespace }}
spec:
  replicas: {{ .Values.nlpingestion.api.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.nlpingestion.api.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.nlpingestion.api.name }}
    spec:
      containers:
        - name:  {{ .Values.nlpingestion.api.name }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          image: {{ .Values.nlpingestion.api.image }}
          env:
            - name: APP_PATH
              value: /data/results/data
          volumeMounts:
            - mountPath: /data/results
              name: resultsui-pv
      volumes:
        - name: resultsui-pv
          persistentVolumeClaim:
            claimName: resultsui-pv-claim